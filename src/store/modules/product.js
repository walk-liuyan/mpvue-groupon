// import * as types from '../mutation-types'
// import api from '@/utils/api'

const initialState = {
  gets: {
    data: []
  }
}

export default {
  state: initialState,
  actions: {},
  mutations: {}
}
